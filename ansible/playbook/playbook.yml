- hosts: all
  tasks:
  - name: Ping all server
    ansible.builtin.ping:

  - name: Create a directory if it does not exist
    ansible.builtin.file:
      path: /tmp/jagaban-jagaban
      state: directory
      mode: '0755'
  
  - name: List files on remote server
    ansible.builtin.shell: ls /tmp/